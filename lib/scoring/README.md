# `lib/scoring/`

Empty placeholder for Phase 2 (`0.2.1`). Per `docs/architecture/adr-0002-geodata-stack.md`,
scoring functions here are pure TypeScript over already-computed `criterion_value` rows —
arithmetic and comparison, never geometry or raster math. See
`docs/architecture/roadmap-to-first-deployment.md` §4.3.
